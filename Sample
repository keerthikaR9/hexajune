-- Field 4: CreditAccount
-- If IBAN available use IBAN, else use local account number
-- No hyphens/spaces/special chars per Bank Sohar spec
NVL(
  REGEXP_REPLACE(suppl_bank.iban_code, '[^0-9A-Za-z]', ''),
  suppl_bank.BeneficiaryAccountNumber
)  AS credit_account,
